# 我的个人博客系统

一个基于 React + Supabase 构建的现代化个人博客系统，支持文章发布、评论等功能。

## ✨ 功能特性

- 📝 **文章管理**: 创建、编辑、删除文章
- 💬 **评论系统**: 为每篇文章添加评论
- 📱 **响应式设计**: 完美适配手机、平板和桌面设备
- 🎨 **现代化UI**: 美观的界面设计，流畅的用户体验
- ⚡ **快速加载**: 基于 Vite 构建，极速启动

## 🛠️ 技术栈

- **前端框架**: React 18
- **构建工具**: Vite
- **路由**: React Router v6
- **后端服务**: Supabase (PostgreSQL)
- **部署平台**: Netlify
- **样式**: 原生 CSS (现代设计)

## 📋 数据库结构

项目使用 Supabase 作为后端，需要以下数据表：

### users 表
- `id` (uuid, 主键)
- `username` (text)
- `created_at` (timestamptz)

### posts 表
- `id` (uuid, 主键)
- `title` (text)
- `content` (text)
- `author_id` (uuid, 外键 -> users.id)
- `created_at` (timestamptz)

### comments 表
- `id` (uuid, 主键)
- `content` (text)
- `post_id` (uuid, 外键 -> posts.id)
- `created_at` (timestamptz)

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

创建 `.env` 文件（参考 `.env.example`）：

```env
VITE_SUPABASE_URL=你的Supabase项目URL
VITE_SUPABASE_ANON_KEY=你的Supabase匿名密钥
```

### 3. 获取 Supabase 配置信息

1. 登录 [Supabase](https://supabase.com)
2. 进入你的项目
3. 点击 Settings → API
4. 复制 `Project URL` 和 `anon public` key

### 4. 运行开发服务器

```bash
npm run dev
```

访问 http://localhost:3000

### 5. 构建生产版本

```bash
npm run build
```

## 📦 部署到 Netlify

### 方法一：通过 Netlify CLI

1. 安装 Netlify CLI：
```bash
npm install -g netlify-cli
```

2. 登录 Netlify：
```bash
netlify login
```

3. 初始化项目：
```bash
netlify init
```

4. 部署：
```bash
netlify deploy --prod
```

### 方法二：通过 GitHub 连接

1. 将代码推送到 GitHub
2. 登录 [Netlify](https://netlify.com)
3. 点击 "New site from Git"
4. 选择你的仓库
5. 配置构建设置：
   - Build command: `npm run build`
   - Publish directory: `dist`
6. 添加环境变量：
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
7. 点击 "Deploy site"

### 方法三：拖拽部署

1. 运行 `npm run build` 构建项目
2. 将 `dist` 文件夹拖拽到 Netlify 的部署区域

## 📝 使用说明

### 创建文章

1. 点击导航栏的 "写文章"
2. 输入标题和内容
3. 点击 "发布文章"

### 查看文章

1. 在首页浏览所有文章
2. 点击文章卡片查看详情

### 添加评论

1. 在文章详情页底部
2. 输入评论内容
3. 点击 "发表评论"

### 编辑/删除文章

在文章详情页可以编辑或删除文章

## 🔧 项目结构

```
myhomeworkblog/
├── public/              # 静态资源
├── src/
│   ├── components/      # 组件
│   │   └── Navbar.jsx   # 导航栏
│   ├── lib/            # 工具库
│   │   └── supabase.js # Supabase 客户端
│   ├── pages/          # 页面
│   │   ├── Home.jsx    # 首页
│   │   ├── PostDetail.jsx # 文章详情
│   │   ├── CreatePost.jsx # 创建/编辑文章
│   │   └── About.jsx   # 关于页面
│   ├── App.jsx         # 主应用组件
│   ├── App.css         # 应用样式
│   ├── main.jsx        # 入口文件
│   └── index.css       # 全局样式
├── index.html          # HTML 模板
├── package.json        # 项目配置
├── vite.config.js      # Vite 配置
└── README.md           # 说明文档
```

## 📄 许可证

MIT License

## 👤 作者

学生项目

---

**注意**: 请确保在部署前正确配置 Supabase 环境变量，否则应用将无法正常工作。

